pandas>=2.0.0
numpy>=1.24.0
geopandas>=0.13.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0
shapely>=2.0.0
folium>=0.14.0
streamlit>=1.28.0
python-dotenv>=1.0.0

# gwlearn is optional for enhanced GWR modeling and not currently available
# on the supported Python versions used by hosted deployments. The code
# gracefully falls back to LinearRegression when it is unavailable.
